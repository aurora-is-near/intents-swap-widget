on: workflow_call

jobs:
  checks:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4
      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version-file: package.json
      - name: Install
        run: yarn install --frozen-lockfile --non-interactive
      - name: Build
        run: yarn build --filter=@aurora-is-near/intents-swap-widget
      - name: Lint
        run: yarn lint
      - name: Check types
        run: yarn typecheck
      - name: Test
        run: yarn test
