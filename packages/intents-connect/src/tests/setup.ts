// Registers the jest-dom matchers (toHaveTextContent, …). The shared eslint
// config's jest-dom plugin auto-fixes assertions into these matchers, so they
// have to be available.
import '@testing-library/jest-dom/vitest';
