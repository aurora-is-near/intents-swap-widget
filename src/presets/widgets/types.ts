// String literal unions instead of const objects for consistency
export type TokenInputType = 'source' | 'target';
export type TokenModalState = 'source' | 'target' | 'none';
export type QuoteType = 'exact_in' | 'exact_out';
