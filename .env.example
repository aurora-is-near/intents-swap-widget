# Reown (WalletConnect) Project ID - REQUIRED for multi-chain wallets
# Get your project ID from https://cloud.reown.com
VITE_REOWN_PROJECT_ID=your_project_id_here

# App URL - Used for wallet metadata
# Local development
# VITE_APP_URL=http://localhost:5173
# Production
# VITE_APP_URL=https://your-production-domain.com

# ============================================================================
# TON Connect Configuration
# ============================================================================
# TON Connect manifest URL is auto-generated from window.location.origin
#
# Browser wallets work with localhost
# Mobile wallets (Telegram Wallet) need public URL
#
# To test mobile wallets, deploy to fly.io:
#   fly deploy --ha=false
#   Demo: https://intents-swap-widget-demo.fly.dev/ton
#   Update public/tonconnect-manifest.json with your fly.io URL
#
# Note: Dockerfile, .dockerignore, and fly.toml are only for fly.io deployment.
#       Remove them if public URL testing is no longer needed.
# ============================================================================
